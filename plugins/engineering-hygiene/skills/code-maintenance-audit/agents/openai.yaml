interface:
  display_name: "Code Maintenance Audit"
  short_description: "Dead-code and duplication sweep"
  default_prompt: "Use $code-maintenance-audit to review these code changes for dead code, stale leftovers, duplication, and safe consolidation opportunities."
policy:
  allow_implicit_invocation: false
