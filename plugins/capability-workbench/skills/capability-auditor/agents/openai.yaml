interface:
  display_name: "Capability Auditor"
  short_description: "Audit capability artifact safety and source evidence"
  default_prompt: "Use Capability Auditor to inspect a skill, plugin, guidance, or trigger artifact for static risks, evidence coverage, install scope, token cost, duplicated logic, and validation readiness. Use Capability Evaluation for behavior against a baseline."
