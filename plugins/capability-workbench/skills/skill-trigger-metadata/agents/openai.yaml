interface:
  display_name: "Skill Trigger Metadata"
  short_description: "Design skill names and descriptions that trigger reliably."
  default_prompt: "Use Skill Trigger Metadata to design or debug a skill name and description so the host agent loads the right SKILL.md."
