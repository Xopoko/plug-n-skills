interface:
  display_name: "Agent Harness Evaluation"
  short_description: "Evaluate agent harness behavior and releases."
  default_prompt: "Use Agent Harness Evaluation when a benchmark, replay, regression diagnosis, or release gate must test harness behavior, even if buried inside a broader rollout request. Build and validate an evaluation artifact; do not use this for scholarly surveys, generic unit tests, prompt-only benchmarks, or designing the harness without an empirical evaluation deliverable."
