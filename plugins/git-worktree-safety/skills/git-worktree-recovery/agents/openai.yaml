interface:
  display_name: "Git Worktree Recovery"
  short_description: "Git pointers recover without changing Git state"
  default_prompt: "Use $git-worktree-recovery to audit this missing worktree or broken convenience symlink and determine whether an exact pointer repair is safe."
