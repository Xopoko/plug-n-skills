interface:
  display_name: "GitLab Review Response"
  short_description: "GitLab threads get exact-head fixes"
  default_prompt: "Use $gitlab-review-response to address this GitLab merge request's review threads and reply with exact-head evidence."
