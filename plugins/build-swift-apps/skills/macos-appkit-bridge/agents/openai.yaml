interface:
  display_name: "macOS AppKit Bridge"
  short_description: "Bridge SwiftUI into AppKit for native macOS behavior"
  default_prompt: "Use $macos-appkit-bridge to choose the smallest AppKit bridge, keep SwiftUI as the source of truth, and implement macOS-specific behavior that SwiftUI does not model cleanly."
