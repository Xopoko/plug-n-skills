interface:
  display_name: "macOS Signing Inspector"
  short_description: "Inspect codesign, entitlements, and Gatekeeper failures"
  default_prompt: "Use $macos-signing-inspector to inspect a macOS app or binary, classify the signing or entitlement problem, and explain the minimum fix path."
