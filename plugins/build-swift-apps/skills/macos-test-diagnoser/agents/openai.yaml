interface:
  display_name: "macOS Test Diagnoser"
  short_description: "Run and explain macOS test failures with focused reruns"
  default_prompt: "Use $macos-test-diagnoser to run the smallest meaningful macOS test scope first, classify failures correctly, and summarize the next best rerun or fix."
